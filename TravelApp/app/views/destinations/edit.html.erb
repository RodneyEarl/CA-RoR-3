<div class="container">

	<%= form_for(@destination) do |f| %>
		<div class="field">
			<%= f.text_field :name %> <br>
			<%= f.text_field :image %> <br>
			<%= f.text_area :description %> <br>
		</div>
		<div class="actions"> 
    		<%= f.submit "Update" %> 
 		</div> 
	<% end %>
</div>